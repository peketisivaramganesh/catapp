apiVersion: apps/v1
kind: Deployment
metadata:
  name: catapp-ganesh
  labels:
    app: catapp-ganesh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catapp-ganesh
  template:
    metadata:
      labels:
        app: catapp-ganesh
    spec:
      containers:
        - name: catapp-ganesh
          # This image will be overwritten by the update-deployment Task
          image: catapp:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
