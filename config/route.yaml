apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: catapp-ganesh
  name: catapp-ganesh
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: catapp-ganesh
